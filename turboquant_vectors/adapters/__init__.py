"""
Optional adapters for popular frameworks.

Each adapter is import-guarded — the framework dependency is only
required when you actually import the adapter.

Usage:
    from turboquant_vectors.adapters.langchain_adapter import PrivateEmbeddings
"""
